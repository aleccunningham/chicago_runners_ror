<%= form_with(model: promotion, local: true) do |form| %>
  <% if promotion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(promotion.errors.count, "error") %> prohibited this promotion from being saved:</h2>

      <ul>
      <% promotion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :promotion_title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :promotion_description %>
  </div>

  <div class="field">
    <%= form.label :imagePath %>
    <%= form.text_field :imagePath, id: :promotion_imagePath %>
  </div>

  <div class="field">
    <%= form.label :startDate %>
    <%= form.date_select :startDate, id: :promotion_startDate %>
  </div>

  <div class="field">
    <%= form.label :endDate %>
    <%= form.date_select :endDate, id: :promotion_endDate %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
